version: "3.9"

services:
  web:
    image: node:17-alpine
    container_name: piyomiru-web
    working_dir: /app
    volumes:
      - ./web:/app
    ports:
      - 3000:3000
    command: >
      sh -c "
        npm install &&
        npm run start
      "

  api:
    image: node:17-alpine
    container_name: piyomiru-api
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - 1337:1337
    command: >
      sh -c "
        yarn &&
        yarn develop
      "